<project name="br.ufsc.labsec.libcryptosec" default="dist" basedir=".">
	<description>
		Libcryptosec Java Build
	</description>

	<!-- Global Properties -->
	<property name="src" location="src"/>
	<property name="build" location="bin"/>
	<property name="include" location="LibcryptosecJNI/include"/>

	<!-- classpath setup -->
	<path id="classpath">
		<fileset dir="src/br/ufsc/labsec/libcryptosec">
			<include name="**/*.java" />
		</fileset>
	</path>

	<!-- Targets -->
	<target name="init">
		<tstamp/>
		<pathconvert pathsep=" " property="echo.classpath" refid="classpath"></pathconvert>
		<exec executable="sh" outputproperty="classes">
			<arg line="find_classnames.sh ${echo.classpath}" />
		</exec>
		<echo>message="classpath: ${echo.classpath}"</echo>
		<echo>message="classpath: ${classes}"</echo>
	</target>

	<target name="compile-java" depends="init" description="compile java">
		<mkdir dir="${build}" />
		<javac srcdir="${src}" destdir="${build}" />
	</target>

	<target name="compile" depends="compile-java" description="compile the source">
		<!--javah-->
		<mkdir dir="${include}"/>
		<javah destdir="${include}" class="${classes}" classpath="bin"/>
	</target>

	<target name="dist" depends="compile" description="generate the distribution" >
		<!--make .so-->
		<exec executable="sh">
			<arg line="organize_files.sh LibcryptosecJNI/include" />
		</exec>
		<echo>Message: "Files Filtered"</echo>
		<exec executable="qmake" dir="LibcryptosecJNI" failonerror="true"/>
	</target>

	<target name="clean" description="clean up" >
		<!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${include}"/>

		<!--make .so-->
		<exec executable="make" dir="natives" failonerror="true">
			<arg line="clean"/>
		</exec>
	</target>
</project>
